# Core (no external deps beyond these are required for app/core, app/data,
# app/agents, app/analytics, app/llm/mock_client.py, app/reports -- i.e.
# everything covered by tests/ and eval/):
pandas>=2.1
numpy>=1.26
reportlab>=4.0
openpyxl>=3.1        # .xlsx read/write support (app/data/loader.py) -- genuinely tested in this project

# Optional: legacy .xls (old binary Excel format) support
# xlrd>=2.0

# Web API layer (app/api/):
fastapi>=0.110
uvicorn[standard]>=0.29
pydantic>=2.6
python-multipart>=0.0.9

# Frontend (frontend/streamlit_app.py):
streamlit>=1.33
plotly>=5.20

# Testing (tests/, eval/):
pytest>=8.0

# ============================================================
# LLM Providers
# ============================================================

# Primary LLM provider (cloud-native)
google-generativeai>=0.8    # Gemini Flash/Pro

# Fallback LLM provider (and existing Groq support)
openai>=1.0                  # Used by GroqLLMClient (OpenAI-compatible endpoint)

# Optional: Anthropic Claude
anthropic>=0.25

# ============================================================
# Cloud / AWS
# ============================================================

boto3>=1.34                  # AWS SDK — S3, Lambda, CloudWatch
watchtower>=3.0              # CloudWatch log handler for Python logging

# ============================================================
# Database / Migrations
# ============================================================

sqlalchemy>=2.0              # ORM for PostgreSQL/RDS metadata database
alembic>=1.13                # Database migrations
psycopg2-binary>=2.9         # PostgreSQL adapter (production/RDS)

# ============================================================
# Optional analytical backends
# ============================================================

# duckdb>=0.10               # Swap DATABASE_BACKEND=duckdb instead of sqlite

# ============================================================
# Lint
# ============================================================

ruff>=0.4
